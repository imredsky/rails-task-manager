<h2><%= @task.title %></h2>
<p><%= @task.details%></p>

<%= link_to "Edit your task", edit_task_path(@task) %>
<%= link_to "Delete your task",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure about that?"} %>
